node_modules
dist
dist-ssr
coverage
tsconfig.tsbuildinfo
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Local-only — never push to the public repo.
.claude/
SESSION-NOTES.md

# Discovery snapshots from scripts/discover-list.ps1 — dumped SharePoint content.
# Useful locally, not something to commit; the schema itself is documented in CLAUDE.md.
scripts/*-schema.json

# Before/after records from scripts/reassign-site-user.ps1 — real item titles,
# and the only rollback reference for an applied run. Keep locally, don't commit.
scripts/reassign-*.csv
